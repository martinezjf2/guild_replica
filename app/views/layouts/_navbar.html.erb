





    <% if !current_student %>

<nav class="navbar navbar-expand-lg navbar-light ">
  <a class="navbar-brand guildLogo" href="/"><%= image_tag("/students/guild_logo.jpeg", width: '100px') %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse justify-content-end" id="navbarTogglerDemo02">
    <ul class="navbar-nav navLinksLogSign">
      <li class="nav-item">
        <%= link_to 'Student Login', login_path, class: "nav-link btn" %>
      </li>
      <li class="nav-item">
        <%= link_to 'Student Signup', signup_path, class: "nav-link btn" %>
      </li>
      </ul>
  </div>
  </nav>
      <% else %>
      <nav class="navbar">
  <a class="navbar-brand guildLogo" href="/students/<%= current_student.id%>"><%= image_tag("students/guild_logo.jpeg", width: '100px') %></a>
          <div class="dropdown nav-item">
          <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Hi, <%= current_student.first_name %>
          </button>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
          <%= link_to "View Courses", student_courses_path(current_student), class: "btn dropdownLink" %>
          <%= link_to "Add Course", new_student_course_path(current_student), class: "btn dropdownLink" %>

          <%= link_to "Edit Profile", edit_student_path(current_student), class: "btn dropdownLink" %>
          <%= link_to "Log Out", logout_path, method: :delete, data: {confirm: "Really?" }, class: "btn dropdownLink" %>
        </div>
      </div>
        </nav>
      <%end%>
